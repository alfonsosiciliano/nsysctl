NSYSCTL(8)              FreeBSD System Manager's Manual             NSYSCTL(8)

NAME
     nsysctl - get or set kernel state

SYNOPSIS
     nsysctl [--libxo opts [-r tagname]] [-DdFIilmNpqTtWy [V|v [h [b|o|x]]]]
             [-e sep] [-B bufsize] [-f filename] name[=value] ...
     nsysctl [--libxo opts [-r tagname]] [-DdFIlmNpqSTtWy [V|v [h [b|o|x]]]]
             [-e sep] [-B bufsize] -A|a|X

DESCRIPTION
     The systems maintains a "Management Information Base" ("MIB") of kernel
     states.  The nsysctl tool can get the value and allows users with
     appropriate privilege to set kernel state.

     nsysctl shows only the information explicitly indicated by the options
     (see COMPATIBILITY for a comparison with sysctl(8).

     The following options are available:

     --libxo options
             Generate output via libxo(3) in a selection of different human
             and machine readable formats.  See xo_parse_args(3)

     -A      [DOC]

     -a      [DOC]

     -b      [DOC]

     -B bufsize
             [DOC]

     -D      [DOC]

     -d      [DOC]

     -e sep  [DOC]

     -F      [DOC]

     -f filename
             [DOC]

     -h      [DOC]

     -I      [DOC]

     -i      [DOC]

     -l      [DOC]

     -m      [DOC]

     -N      [DOC]

     -o      [DOC]

     -p      [DOC]

     -q      [DOC]

     -r tagname
             [DOC]

     -S      [DOC]

     -T      [DOC]

     -t      [DOC]

     -V      [DOC]

     -v      [DOC]

     -W      [DOC]

     -X      [DOC]

     -x      [DOC]

     -y      [DOC]

EXIT STATUS
     The nsysctl utility exits 0 on success, and >0 if an error occurs.

EXAMPLES
     To get the hostname:

           nsysctl -Nv kern.hostname

     To set the hostname:

           nsysctl kern.hostname=myBSD

     Dump all info about a state:

           nsysctl -NlydtmFv kern.ostype

     Dump all info about a state in xml format:

           nsysctl --libxo=xml,pretty -r -NlydtmFv kern.ostype

COMPATIBILITY
     The -w option has been deprecated and is silently ignored.

     % sysctl name

           % nsysctl -Nv name

     % sysctl -a:

           % nsysctl -NVa

     % sysctl -na:

           % nsysctl

SEE ALSO
     sysctl(3), sysctlmibinfo(3), loader.conf(5), sysctl.conf(5)

HISTORY
     The nsysctl utility first appeared in FreeBSD 13.0.

AUTHORS
     The nsysctl utility and this manual page were written by Alfonso S.
     Siciliano <alf.siciliano@gmail.com>

BUGS
     % nsysctl libxo=xml debug.witness.fullgraph -> segmentation fault

FreeBSD 13.0-CURRENT           January 25, 2019           FreeBSD 13.0-CURRENT
